# Backend API base URL (no trailing slash). Create React App only exposes env vars prefixed with REACT_APP_.
# Copy to .env.local for local overrides (do not commit .env.local).
REACT_APP_API_URL=http://localhost:3010
